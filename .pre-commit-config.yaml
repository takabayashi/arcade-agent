# Run with: pre-commit run --all-files
# Install once: pre-commit install (adds the .git/hooks/pre-commit hook)
repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.14  # keep in sync with pyproject's [project.optional-dependencies] dev pin
    hooks:
      - id: ruff-check
        args: [--fix]
      - id: ruff-format

  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-toml
      - id: check-merge-conflict
      - id: check-added-large-files
        args: [--maxkb=500]
